<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="user">

	<insert id="insert" parameterType="uservo">
		<![CDATA[
			insert into user values(null, #{id}, #{password}, #{name}, #{address},#{addressDetail},#{phoneNumber},#{email},'gold',now(),'test')
		]]>
		<selectKey keyProperty="no" resultType="Long" order="AFTER">
			<![CDATA[
				select last_insert_id()
			]]>
		</selectKey>
	</insert>
	
	<delete id="deleteAll">
		delete from user
	</delete>
	
	<select id="getByEmailAndPassword" parameterType="map" resultType="uservo">
		<![CDATA[
			select no, name from user 
			where email=#{email} and password=#{password}
		]]>
	</select>
	
	<select id="getByNo" parameterType="long" resultType="uservo">
		<![CDATA[
			select no, name from user 
			where no = #{no}
		]]>
	</select>
	
	<update id="update" parameterType="uservo">
		<![CDATA[
			update user set 
			name = #{name}
			]]>
			<if test='password !=""'>
			, password = #{password}
			</if>
			, gender = #{gender}
			where no = #{no}
	</update>
	
	<select id="checkId" parameterType="string" resultType="uservo">
		<![CDATA[
			select  member_no as memberNo, id, name, email, address, address_detail as addressDetail, phone_number as phoneNumber , grade, join_date as joinDate from user 
			where id = #{id}
		]]>
	</select>
</mapper>
